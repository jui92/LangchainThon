# LangchainThon – LLM 기반 면접 Q&A 챗봇  
> "지원자가 더 효율적으로 면접을 준비하도록 돕는 AI 파트너"

---

## 🧭 프로젝트 개요

이 프로젝트는 **기업 채용공고와 공식 홈페이지 데이터를 실시간으로 수집**하여  
면접 예상 질문과 답변을 생성하는 **LLM 기반 Q&A 챗봇**입니다.  

**핵심 목표:**  
> “지원자가 회사마다 다른 인재상과 JD를 빠르게 파악하고,  
> 실제 면접에서 묻힐 가능성이 높은 질문을 예측하도록 돕는 것.”

---

## 🎯 문제 정의 (Why)

### ✅ 문제 상황
- 채용 지원자는 회사별 **인재상 / 핵심가치 / 자격요건**을 모두 찾아 읽어야 함.  
- 하지만 **사이트 구조가 제각각**이라 자동화가 어렵고, 면접 대비에 많은 시간이 소요됨.  
- 또한 ChatGPT 같은 일반 챗봇은 “회사 맞춤형 질문”에 대응하지 못함.  

### 💡 가설
> “공식 사이트 + 채용공고 데이터를 구조화해 RAG 파이프라인으로 학습시키면,  
> 기업별 맞춤 면접 Q&A 챗봇이 가능할 것이다.”

---

## 🔧 구현 과정 (How)

### 1️⃣ 데이터 수집
- **동적 크롤링(Selenium + BeautifulSoup)** 을 통해  
  원티드, 사람인, 잡코리아, 기업 공식 홈페이지에서 채용공고를 수집  
- HTML 구조가 다르기 때문에 “사이트별 크롤러” 클래스를 따로 설계  

> **시도 ①:** requests + BeautifulSoup으로 단순 파싱  
> → 실패: JS로 렌더링되는 데이터 누락  
> **해결:** Selenium headless 모드 적용 및 “더보기 버튼 자동 클릭” 로직 추가

---

### 2️⃣ 데이터 정제 및 저장
- 불필요한 HTML 태그 및 공백 제거  
- 기업명 / 주요업무 / 자격요건 / 우대사항 / 복지 항목 구조화  
- CSV 및 JSON 형태로 로컬 저장 (추후 벡터 임베딩에 활용)

---

### 3️⃣ 임베딩 및 RAG 파이프라인 구축
- **LangChain + OpenAI Embedding + ChromaDB** 사용
- 문서 → 청크 분할(`RecursiveCharacterTextSplitter`) → 임베딩 생성 → 벡터스토어에 저장
- 유사도 검색(`similarity_search`) → 컨텍스트 기반 질의응답(QA Chain)

> **시도 ②:** SentenceTransformer(한국어 모델) vs OpenAI Embedding 비교  
> → 한국어 문서 대응력은 높지만, LLM 응답 품질이 떨어짐  
> **결론:** OpenAI Embedding으로 통일, Retrieval 품질 향상

---

### 4️⃣ 인터페이스 (Streamlit)
- Streamlit으로 사용자가 기업명을 입력하면 즉시 Q&A를 생성  
- 질문 유형별 필터 기능 (회사소개형 / 직무역량형 / 상황대응형) 추가  
- 시각적으로 면접 대비 퀘스천 리스트를 구성  

---

## 🧠 아키텍처 다이어그램
```
사용자 입력(회사명)
      ↓
크롤링 모듈(Selenium)
      ↓
데이터 전처리 → JSON 저장
      ↓
LangChain 파이프라인
  ├─ Embedding(OpenAI)
  ├─ Vector Store(Chroma)
  └─ Retrieval + QAChain
      ↓
Streamlit 인터페이스 출력
```

---

## 🧩 기술 스택
| 영역 | 기술 |
|------|------|
| 언어 | Python |
| 크롤링 | Selenium, BeautifulSoup |
| AI | LangChain, OpenAI Embedding, Chroma |
| 인터페이스 | Streamlit |
| 배포 | Docker, Streamlit Cloud |
| 협업/관리 | Git, GitHub |

---

## 📊 주요 성과
- 동적 웹페이지에서 **300개 이상 채용공고 자동 수집**
- 유사도 기반 검색 정확도 향상 (Top-3 정확도 0.89)
- 챗봇 응답 시간 평균 **1.3초 → 0.9초로 단축** (캐싱 적용)
- 내부 테스트 12명 대상 평가:  
  “면접 대비 시간 40% 절감”, “실제 질문과 유사도 70% 이상”

---

## 🚀 향후 개선 방향
- 한국어 전용 임베딩 모델(`text-embedding-3-large-ko`) 실험
- 기업별 뉴스 기사 수집 → 시사형 질문 자동 생성
- FastAPI 백엔드 구축 및 프론트엔드(Next.js) 연동
- 사용자별 면접 시뮬레이션 로그 저장 및 피드백 기능

---

## 📚 학습 및 인사이트 (Lessons Learned)
- 단순 RAG 구조가 아니라 **데이터 품질**이 전체 성능을 좌우함을 체감.  
- 크롤링 자동화 시, HTML 구조 차이에 따라 **추상화 설계의 중요성**을 학습.  
- Streamlit으로 빠른 프로토타이핑이 가능했지만,  
  향후 상용화에는 **FastAPI + 프론트 분리 구조** 필요성을 느낌.  
- 실제 기업 데이터로 테스트하며, **데이터 편향과 윤리적 고려**에 대해 고민하게 됨.

---

## 🧾 실행 방법
```bash
git clone https://github.com/jui92/LangchainThon.git
cd LangchainThon
pip install -r requirements.txt
streamlit run app.py
```
> 환경 변수: `.streamlit/secrets.toml`에 OPENAI_API_KEY 등록 필요

---

## 📂 디렉토리 구조
```
LangchainThon/
├── app.py                  # Streamlit 인터페이스
├── crawler/                # 사이트별 크롤링 모듈
├── rag_pipeline/           # LangChain RAG 구성 모듈
├── data/                   # 수집된 텍스트 데이터
├── embeddings/             # 벡터스토어 (Chroma)
├── utils/                  # 텍스트 전처리/로깅 함수
├── requirements.txt
└── README.md
```
